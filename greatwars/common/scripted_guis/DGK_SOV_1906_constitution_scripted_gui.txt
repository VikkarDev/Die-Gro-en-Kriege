scripted_gui = {
	DGK_SOV_constitution_1906_scripted_gui = { # name of the scripted gui type
		window_name = "SOV_constitution_container" # name of the window
		context_type = player_context # multiple versions, but shows up as a window regardless

		visible = {
			has_country_flag = SOV_constitution_container_open # if it has this, it'll open the file
		}

		effects = {
			exit_click = {
				clr_country_flag = SOV_constitution_container_open
			}
		}
	}
}